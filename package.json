{
  "name": "flashpay-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace @flashpay/frontend run dev",
    "build:frontend": "npm --workspace @flashpay/frontend run build"
  }
}
